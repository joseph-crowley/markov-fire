# Project TODOs

- Integrate richer firefighting resource models (delays, terrain-aware strategies).
- Add user-defined environment uploads (wind rasters, vegetation maps).
- Build reporting dashboards in `reports` app for comparative analytics.
- Implement automated validation suite comparing model outputs to benchmark datasets.
- Optimize simulation kernel for large grids (vectorisation, Numba, or PyPy experiments).
